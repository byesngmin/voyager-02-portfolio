# Voyager-02 Hermes Fast Path

> 이 파일은 Hermes가 CLAUDE.md보다 우선 로드하는 Hermes 전용 규칙이다.
> Hermes는 first-match로 이 파일을 로드하므로, CLAUDE.md의 하네스(Claude Code용 위임 규칙)와 이 파일을 동시에 실행하지 않는다.

## 1. 오케스트레이션 원칙

- Hermes가 이 저장소의 1차 오케스트레이터이자 기본 실행자다.
- CLAUDE.md에 정의된 "Claude Code는 항상 외부 CLI(codex/gemini)에 위임" 규칙은 Hermes 세션에는 적용하지 않는다.
- Ponytail/minimal-first: 기존 Hermes 도구(read_file, search_files, patch, write_file, terminal, browser, todo)로 해결 가능하면 외부 codex/claude/gemini CLI를 호출하지 않는다.

## 2. 작업 등급 (Task Grade)

| 등급 | 기준 | 처리 방식 |
|------|------|-----------|
| Small | 최대 3개 파일, 예상 수정 120줄 이하, 단일 기능/문구/스타일 | Hermes가 직접 수정·검증. 외부 에이전트 금지 |
| Medium | 4~8개 파일 또는 구조 변경 | 먼저 파일/의존성/검증 명령 확인 후, 정말 필요한 경우에만 leaf subagent 1개 또는 외부 CLI 1회. 중첩 위임 금지 |
| Large | 9개 이상 파일, 아키텍처/대규모 리서치/병렬 독립 작업 | 착수 전 사용자에게 병목, 예상 호출 경로, 검증 비용, 최단 대안을 보고 |

## 3. Preflight / BUDGET

- 외부 에이전트 호출, 배포, 3개 초과 파일 수정, 2분 이상 예상 작업은 실행 전 병목을 보고한다.
- 사용자가 계속 진행을 명확히 지시한 작업은 보고 후 바로 실행하되, 불필요한 확인 질문은 하지 않는다.

## 4. 위임 규칙

- Hermes `delegate_task`는 leaf 작업만, 동시에 1개만 실행한다. nested/orchestrator 위임은 금지한다.
- 외부 CLI가 수행한 작업이 다시 다른 CLI를 호출하게 하지 않는다.
- 동일 범위 재위임 대신 delta correction(수정분만 전달)은 1회만 허용한다.

## 5. 중단 규칙 (ESCALATE)

다음 상황에서는 즉시 중단하고 사용자에게 보고한다:
- 같은 오류가 2회 반복될 때
- 120초 이상 무진행 상태일 때
- 예상 범위를 2배 초과할 때
- 토큰/시간 사용이 급증할 때

보고 형식: `ESCALATE(BUDGET|ERROR|CROSS)` + 사유

## 6. 상태 관리

- Hermes `todo`와 `.agent/HANDOFF.md`만 사용한다.
- Hermes 작업에서 `agentmanager/taskboard.md`, `conflicts.md`, `walkthrough.md`, `changelog.md`를 읽거나 갱신하지 않는다.
- 사용자가 명시하지 않으면 별도 관리 문서를 만들지 않는다.

## 7. 검증 (Verification)

- 구현 중 반복적인 full build/배포는 금지한다.
- targeted check(부분 검증)를 우선 수행한다.
- 마지막에 `git diff --check`와 `npm run build`를 각 1회 실행한다.
- 핵심 route는 브라우저 QA를 1회 수행한다.
- 모든 요구가 끝난 후 커밋/배포는 1회만 수행한다.

## 8. Git 규칙

- force push 금지.
- 사용자 변경 사항을 보존한다.
- 자동 커밋 금지. 사용자가 배포/완료를 명시적으로 요구했거나 작업 흐름상 명백할 때만 검증 후 커밋한다.

## 9. 프로젝트 불변 조건

- 모든 사용자 응답 및 산출물은 한국어로 작성한다.
- React/Vite 구조를 유지한다.
- "보이저 2호" 제목/서사를 유지한다.
- 실제 경험만 사용한다 (허구/과장 금지).
- credentials(자격 증명)은 기록하지 않는다.

## 10. 완료 보고 형식

작업 완료 시 다음을 짧게 보고한다:
- 변경 내용
- 검증 증거
- 남은 작업/경고 (있는 경우)
